{
  "name": "vox-tests",
  "private": true,
  "version": "0.0.0",
  "description": "Playwright e2e tests for the Vox site",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:install": "playwright install chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1"
  }
}
